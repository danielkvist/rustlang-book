// The main function is special because its always
// the first code that runs in every Rust program.
// It doesn't have parameters nor a return.
fn main() {
    // ! calls a macro
    println!("Hello, world!");
}